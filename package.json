{
  "name": "mytest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d ./",
    "serve": "pm2 start index.js -i max",
    "update-server": "pm2 stop index && pm2 kill && git reset --hard && git pull origin droplet-account && npm run build && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "compression": "^1.7.2",
    "cookie-parser": "^1.4.3",
    "dotenv": "^5.0.1",
    "express-domain": "^1.0.5",
    "express-jwt": "^5.3.1",
    "http-proxy": "^1.16.2",
    "jsonwebtoken": "^8.2.0",
    "pug": "^2.0.0-rc.4",
    "socket.io": "^2.0.4"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
